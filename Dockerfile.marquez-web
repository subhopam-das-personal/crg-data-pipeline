FROM marquezproject/marquez-web:0.47.0

# API proxy target — uses Railway private network (no CORS, no public exposure needed)
ENV MARQUEZ_HOST=marquez.railway.internal
ENV MARQUEZ_PORT=5000

# Web server port — must match PORT set on the Railway service
ENV WEB_PORT=3000

EXPOSE 3000
